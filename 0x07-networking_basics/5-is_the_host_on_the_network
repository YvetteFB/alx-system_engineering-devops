#!/bin/bash

# Check if an IP address argument is passed
if [ $# -eq 0 ]; then
  echo "Usage: $0 {IP_ADDRESS}"
  exit 1
fi

# Store the IP address
IP_ADDRESS=$1

# Ping the IP address 5 times
echo "Pinging $IP_ADDRESS 5 times..."
ping -c 5 $IP_ADDRESS
